services:
  website:
    build: .
    ports:
      - "4000:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
      DATA_FILE: /app/server/data/submissions.json
      CLIENT_ORIGIN: http://localhost:4000
    volumes:
      - salon_data:/app/server/data

volumes:
  salon_data:
